# Local Feature Selection with Statistical Guarantees

Senior thesis — Spencer Venancio.

Research chronology and open questions: [`notes/meeting_notes.tex`](notes/meeting_notes.tex).
Conventions and project philosophy: [`CLAUDE.md`](CLAUDE.md).

```bash
pip install -e ".[torch,dev]"
pytest
python -m experiments.run experiments/configs/<name>.yaml
```
